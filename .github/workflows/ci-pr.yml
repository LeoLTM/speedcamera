name: CI - PR

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'headless-rust/**'
      - 'arduino/**'
      - '.github/workflows/**'

jobs:
  changes:
    name: Detect Changed Paths
    runs-on: ubuntu-latest
    outputs:
      headless_rust: ${{ steps.filter.outputs.headless_rust }}
      arduino: ${{ steps.filter.outputs.arduino }}
    steps:
      - uses: actions/checkout@v6

      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            headless_rust:
              - 'headless-rust/**'
              - '.github/workflows/**'
            arduino:
              - 'arduino/**'
              - '.github/workflows/**'

  test:
    needs: changes
    if: needs.changes.outputs.headless_rust == 'true'
    uses: ./.github/workflows/_test.yml
    secrets: inherit

  build:
    needs: [changes, test]
    if: ${{ !cancelled() && !failure() && (needs.changes.outputs.headless_rust == 'true' || needs.changes.outputs.arduino == 'true') }}
    uses: ./.github/workflows/_build.yml
    with:
      version: ''
      build-headless: ${{ needs.changes.outputs.headless_rust == 'true' }}
      build-firmware: ${{ needs.changes.outputs.arduino == 'true' }}
    secrets: inherit
